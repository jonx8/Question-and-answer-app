spring:
  datasource:
    url: jdbc:tc:postgresql:16:///questions?TC_DAEMON=true
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test

  liquibase:
    drop-first: true